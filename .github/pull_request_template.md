### Description
Briefly describe what your new agent does.

### Checklist
- [ ] I have placed my `.yml` file in an allowed category folder (e.g. `agents/frontend/`).
- [ ] I included the `metadata` block with the `{{authors}}` / `{{published_at}}` variables.
- [ ] I ran `pnpm run validate` locally and it passed.
- [ ] My `id` and `name` are unique.
